import React from 'react'
import ReactDOM from 'react-dom/client'

import App from './App';

// rendering the app component
// typically react apps are all components
ReactDOM.createRoot(document.getElementById('root')).render(<App />);
